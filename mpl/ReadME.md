"Modern Programming Languages"



belowten = Calculates the sum of all multiples of 3 or 5 below 1000 (Project Euler Problem 1).



bigthree = Generates three random numbers and calculates the biggest value among them.



dices = Performs a Monte Carlo simulation by rolling two dice 1 million times to analyze the probability distribution of their sums.



faktor = Calculates the factorial of a given number using a simple iterative for loop.



faktortest = Implements unit tests using Python's unittest framework to verify the accuracy of the factorial function.



kodlar: Defines basic utility functions including triangle validation, manual power calculation, and addition.



main: Demonstrates list manipulation and aggregation by modifying a meeting list and calculating the total number of participants.



myadd: Tests the add function using Python’s unittest framework to ensure correct behavior.



power-uslusayilar: Calculates the power of a number using both built-in exponentiation and tests the my\_power function with unittest.



probability = A Monte Carlo simulation (revisited) that analyzes the probability distribution of the sum of two dice rolling 1 million times.



randomlist = Demonstrates basic list manipulation by generating random numbers, appending items, and calculating the rounded average.



reversed = Demonstrates iterating through a list in both forward and reverse directions using for loops and index manipulation.



smallthree = Generates three random numbers and calculates the smallest value among them.



startstopstep: Prints the elements of a list in both forward and reverse order using start, stop, and step logic in a loop.



testTriangles: Uses Python’s unittest framework to automatically verify the checkTriangle function with multiple test cases.



triangel-python: Prompts the user for triangle sides and determines interactively whether a valid triangle can be formed.

